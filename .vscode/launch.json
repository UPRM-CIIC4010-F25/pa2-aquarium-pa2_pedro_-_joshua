{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "oF: Build & Launch (Release)",
      "type": "cppdbg",
      "request": "launch",
      "MIMode": "lldb",
      "preLaunchTask": "build",
      "program": "${workspaceFolder}/bin/${workspaceFolderBasename}.app/Contents/MacOS/${workspaceFolderBasename}",
      "cwd": "${workspaceFolder}"
    }
  ]
}
